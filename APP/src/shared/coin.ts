export type Coin = {
    id: string
    name: string
    symbol: string
    image: string
    current_price: string
    price_change_percentage_24h: number
    total_volume: string
    favorite: boolean
}